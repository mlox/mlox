To build a distribution package for the mlox application:

% make mlox

To build a distribution package for the latest rule-base:

% make rules

To build a distribution package for the test suite:

% make test

To build all:

% make
