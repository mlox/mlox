Examples of how to use mlox from the command line

#run mlox in command line mode to ONLY CHECK your load order
python mlox.py -c > mlox_output.out

#run mlox in command line mode to check AND UPDATE your load order
python mlox.py -u > mlox_output.out
